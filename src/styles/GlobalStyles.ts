import { css } from '@emotion/react';
import { GGAMJA_FONT } from './Fonts';

export const globalStyles = css`
  ${GGAMJA_FONT}

  * {
    font-family: 'PfStardust30S', sans-serif;
    font-weight: 400;
  }

  body {
    margin: 0;
    padding: 0;
  }
`;
